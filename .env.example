# Copy this file to `.env` and fill in the values you need.
# Bun loads `.env` from the project root automatically.
#
# Each connector documents the variables it requires in the README and adds
# them here as it is introduced. Nothing is required for the base setup.
