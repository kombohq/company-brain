pre-commit:
  commands:
    format:
      glob: "*.{js,ts,tsx,json,jsonc,yaml,yml,md,css,html}"
      run: bunx prettier --write {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{js,ts,tsx}"
      run: bunx oxlint {staged_files}
